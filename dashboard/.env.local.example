# URL of the running Aperture instance (no trailing slash)
APERTURE_URL=https://localhost:8081

# Hex-encoded admin macaroon from Aperture's data directory
# (e.g. xxd -ps -u -c 1000 admin.macaroon)
APERTURE_MACAROON=
